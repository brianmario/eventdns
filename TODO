Backend::HTTP
 - Fail gracefully when the result isn't 200 or 404.
 - Validate all input. Send approprate error code on invalid input.
 - Make sure that all names end in '.'

eventdns.rb
 - Write more tests for the EventDns class.
 - Make sure we are sending results with the AA and QR bits set and with the RD bit NOT set.
 - Only allow the types of queries that tinydns would allow (one query per request, etc)
